# SQL source

The initial Ramdani schema was applied only to Supabase project `glyynqeksiqrssrvshba` as migration `ramdani_initial_schema`.

Follow-up migrations on that same project:
- `ramdani_revoke_definer_execute`
- `ramdani_revoke_anon_inquiry_select`

Do not apply these to any other project.
